
= stylesheet_link_tag "jquery.bracket.min.css"


%p{style: 'background-color: white'}
  %div.demo

:javascript
  $(document).ready(function(){
    duels = #{@duels.to_json}
    var doubleElimination = {

    "teams": [
      [duels[0].user_one_id, duels[0].user_two_id],
      [duels[1].user_one_id, duels[1].user_two_id]
    ],
    results : []
    }

    $('.demo').bracket({
      init: doubleElimination,
      save: function(){}

    });
  });
